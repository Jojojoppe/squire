#ifndef __CONFIG_H
#define __CONFIG_H 1
/**
 * @file
 * @section DESCRIPTION
 * The main config file
 */

/**
 * @brief Page size of the system in bytes
 * 
 */
#define PAGE_SIZE 0

/**
 * @brief Start of usable process memory
 * 
 */
#define VMM_USERREGION_BASE 0
/**
 * @brief Length of usable process memory
 * 
 */
#define VMM_USERREGION_LENGTH 0

/**
 * @brief Size of architecture specific data field in proc_proc_t
 * 
 */
#define PROC_PROCDATA_SIZE 0

/**
 * @brief Size of architecture specific data field in proc_thread_t
 * 
 */
#define PROC_THREADDATA_SIZE 0


#include <__IN_ARCH_IN__/config.h>

#endif